# 客运售票管理系统：项目上下文与协作规则

> 本文件是本项目的长期记忆。任何人或 Codex 在修改项目之前必须完整阅读本文件；修改完成后必须同步更新“当前状态”“最近变更”和“验证记录”。

## 1. 项目概况

- 项目名称：客运售票管理系统
- 技术栈：Qt 6、C++、Qt Widgets、CMake、MinGW
- 项目目录：C:\Users\zjw20\QtProjects\bus_ticket_system\bus_ticket_system
- 主要界面文件：widget.ui
- 主要业务代码：widget.cpp
- 类声明文件：widget.h
- 程序入口：main.cpp
- 构建目录：build
- 当前开发目标：通过逐版本迭代学习 Qt、C++、CMake 和 Git，而不是一次性堆砌全部功能。

## 2. 修改工作流（必须遵守）

1. 修改前完整阅读本文件。
2. 检查 Git 状态、最近提交以及相关源文件，不能只依赖本文件中的旧记录。
3. 保留用户已有修改，不覆盖无关内容。
4. Qt Designer 控件的 objectName 必须和 C++ 中 ui-> 后使用的名称完全一致。
5. 修改 widget.ui 后要重新执行 CMake 配置或构建，让 uic 重新生成 ui_widget.h。
6. 功能代码按板块添加中文注释，使初学者能够判断每段代码的职责。
7. 修改后至少执行一次编译；涉及界面或交互时还应运行并进行人工验证。
8. 完成修改后更新本文件的当前版本、功能、重要控件、变更内容和验证结果。
9. 未经用户明确要求，不执行 Git commit、push、reset 或删除历史版本。
10. 若本文件与实际代码或 Git 状态冲突，以实际检查结果为准，并及时修正本文件。

## 3. 当前版本状态

- 当前正在开发：V0.16
- Git 最新已提交版本：V0.14（85c93b4）
- V0.10 状态：多页面运营工作台和售票中心独立选班次已完成、已验证并提交 Git。
- V0.11 状态：班次、席位库存、售票职责已彻底分离；六页面工作台和两级售票选择已完成并提交。
- V0.12 状态：订单组合筛选、按订单改签、站点字典下拉和班次逻辑启停已完成编译与离屏启动验证。
- V0.13 状态：可视化座位选择、座位占用防冲突、订单座位号及改签重新选座已完成并提交。
- V0.14 状态：运营预警、停售与退改规则、近七日分析、筛选结果导出及历史单项模板填充已完成并提交。
- V0.15 状态：新建同车次不同日期班次时自动继承最近一期整套席位配置，已完成编译与离屏启动验证。
- V0.16 状态：数据备份恢复、只读一致性体检、保存失败自动回滚和顶部操作反馈已完成编译与离屏启动验证。
- 当前分支：main
- 远程仓库：GitHub origin/main
- 数据保存方式：QSettings 本地持久化
- 主窗口启动方式：main.cpp 中使用 showMaximized，使界面最大化显示。

## 4. 版本历史

- V0.1：创建客运售票系统项目框架。
- V0.2：实现班次信息录入和表格显示。
- V0.3：实现选中班次删除和删除确认。
- V0.4：实现班次信息修改，并优化自适应布局。
- V0.5：新增搜索功能，包括关键词输入、搜索和显示全部。
- V0.6：优化界面，并实现班次数据自动保存和启动自动读取。
- V0.7：将班次信息扩展为六列，增加发车时间、票价、余票，加入输入校验和班次号重复检查；添加、修改、删除、搜索和持久化均支持六列数据。
- V0.8：增加售票中心，可按数量售票和退票，余票自动联动，并统计班次数、总余票、累计销量和营业额。
- V0.9：升级为专业运营驾驶舱，新增品牌头部、五项指标卡、班次/流水双页签、库存预警、交易流水持久化、即时搜索、清空和 CSV 导出；视觉升级为深蓝、白色和浅灰商业主题。
- V0.10：升级为左侧导航多页面工作台，拆分运营总览、班次管理、售票中心和交易流水，并增加实时售票率、自动运营建议和快捷入口。
- V0.11：新增实名订单管理；班次扩展为车次日期、发到时刻和席位级库存，每种席位拥有独立票价、座位数、余票及售票统计。
- V0.12：新增订单多条件组合筛选与按单改签，引入站点字典和班次逻辑启停。
- V0.13：新增现代化 2+3 车厢座位图，售票按数量选座、已售锁定、订单记录座位号，退票释放座位，改签重新选座。
- V0.14：完善售改退时间规则与重复购票校验，新增运营预警、近七日订单趋势、热门班次排行和筛选结果导出。
- V0.15：新增班次模板整套继承，新日期的同车次自动复制最近一期全部席位类型、票价和容量，库存从零销售状态重新开始。
- V0.16：新增 JSON 全量备份与安全恢复、数据一致性体检、保存失败快照回滚和统一操作反馈。

## 5. V0.7 当前功能

### 班次数据字段

1. 班次号
2. 出发地
3. 目的地
4. 发车时间
5. 票价
6. 余票

### 功能模块

- 添加班次：读取六个输入框，校验后添加到表格。
- 输入校验：检查必填项、HH:mm 时间格式、非负票价和非负整数余票。
- 重复检查：班次号不能重复。
- 修改班次：选择表格行后回填输入框，保存后更新该行。
- 删除班次：要求先选择行，并弹出确认对话框。
- 搜索班次：关键词可匹配六列数据。
- 显示全部：恢复所有被搜索隐藏的行。
- 自动保存：添加、修改和删除后写入 QSettings。
- 自动读取：程序启动时恢复数据，并兼容旧版本三列记录。
- 自适应界面：表格和主要布局随最大化窗口伸展。
- 中文功能注释：widget.cpp 已按初始化、校验、添加、修改、删除、搜索、保存和读取分区。

## 6. V0.8 售票中心功能

- 选中班次后显示班次号、路线、时间、票价、余票和累计销量。
- 可选择 1～99 张票进行售票或退票。
- 售票前检查余票是否充足，成功后减少余票并增加销量、营业额。
- 退票数量不能超过本系统累计售出数量，成功后恢复余票并扣减销量、营业额。
- 售票和退票均有二次确认及完成提示。
- 经营统计显示班次数、总余票、累计售票和营业额。
- 累计销量和营业额通过 QSettings 自动保存，并兼容 V0.7 旧数据。

## 7. V0.9 运营驾驶舱功能

- 顶部品牌区展示系统名称、业务范围和运行状态。
- 五张指标卡展示运营班次、可售余票、累计售票、累计营业额和库存预警。
- “班次运营”和“交易流水”双页签分离管理任务与审计任务。
- 余票为 0 时显示售罄预警，余票 1～5 时显示库存紧张预警，并配有文字提示。
- 每次售票和退票自动生成交易流水，包括时间、类型、班次、路线、数量、单价、金额和交易后余票。
- 交易流水通过 QSettings 持久化，支持任意字段即时搜索。
- 交易流水支持清空；清空操作不会修改班次余票和累计经营统计。
- 支持导出带 UTF-8 BOM 的 CSV 文件，便于使用 Excel 查看中文报表。
- 切换班次时交易数量自动重置为 1。


## 8. V0.10 多页面运营工作台

- 使用左侧导航将系统拆分为“运营总览”“班次管理”“售票中心”“交易流水”四个独立页面。
- 运营总览集中展示五项经营指标，减少业务操作控件对数据阅读的干扰。
- 新增整体售票率进度条，按“累计销量 ÷（累计销量 + 当前余票）”实时计算。
- 新增自动运营建议：根据空数据、库存预警和售票率给出文字化提示，不只依赖颜色。
- 总览页提供班次管理、售票中心和交易流水快捷入口。
- 班次表格双击后自动进入售票中心，缩短售票操作路径。
- 售票中心新增可搜索班次选择器，可直接按班次号或地点选班次，不再要求先进入班次管理。
- 班次管理与售票中心的当前班次双向同步，新增、修改、删除和余票变化后选择器自动刷新。
- 班次增删改查、售票退票、库存联动、流水搜索导出及 QSettings 持久化逻辑全部保留。
- V0.10 继续保留全部 Designer objectName，未修改 widget.ui。

## 9. V0.11 实名订单管理

- 售票中心新增旅客姓名、18 位身份证号和 11 位手机号输入与格式校验。
- 售票成功后生成 `BT + 毫秒时间戳` 唯一订单号，并在成功提示中展示。
- 身份证号和手机号在订单表中脱敏显示，减少界面上的隐私泄露风险。
- 左侧导航新增“订单管理”页面，可搜索订单号、旅客、手机号、班次、行程和状态。
- 订单状态分为“已出票”和“已退票”，已退订单不能重复退款。
- 按订单退票会恢复对应班次余票、销量和营业额，并自动写入交易流水。
- 订单使用独立 `orders` QSettings 数组持久化，兼容 V0.10 及更早版本数据。
- 班次唯一库存键升级为“车次号 + 发车日期 + 席位类型”，同日同车次可维护多种席位。
- 班次表新增发车日期、到达时间、席位类型和座位总数，余票根据售票实时变化。
- 修改座位总数时自动按“座位总数－累计已售”计算余票，并禁止总数小于累计已售。
- 售票选择器、售票确认、交易流水和订单均展示或保存日期、时段与席位。
- 订单退票按车次号、日期和席位三项精确恢复对应库存，避免同车次跨日期或跨席位误退。
- 旧班次自动补当前日期、待补充到达时间和标准座；旧订单自动迁移为历史日期、标准座格式。
- 班次管理现在只维护车次号、发车日期、出发地、目的地、发车时间和到达时间，同一车次日期只显示一行。
- 新增独立“席位库存”页面：选择班次后添加、修改或删除席位，并维护票价、座位总数、已售和余票。
- 新增班次先保存为“未配置席位”状态；未配置席位不计入库存统计，也不允许售票。
- 售票中心改为“先选班次、再选席位”的两级流程，切换班次后仅加载该班次的可售席位。
- 删除最后一个席位只清空库存配置、保留班次；已有销售记录的席位或班次禁止删除。


## 9.1 V0.12 订单与班次治理

- 订单管理由单一搜索框升级为旅客姓名/手机号、订单状态、车次号和下单日期范围组合筛选，支持查询和重置。
- 订单表新增单价和操作列；已出票订单可从行内“改签”按钮或顶部按钮办理改签。
- 改签弹窗按“新班次 + 新席位”两级选择，自动校验余票、计算差价、恢复原库存、扣减新库存，并写入改签补款或退款流水。
- V0.11 旧订单加载时自动使用订单金额和数量推导单价，不破坏已有订单数据。
- 班次管理的出发地和目的地改为可搜索站点下拉框，站点字典保存在 QSettings 的 `stations` 键中。
- 班次新增启用/停用状态并随 routes 数组持久化；停用班次不出现在售票与改签选择器中，历史订单和流水保持可查。
- 原有 Designer 控件 `departureEdit`、`destinationEdit` 保留但隐藏，避免 objectName 兼容问题。
- 到达时间新增「当日到达 / 次日到达」下拉，支持跨天班次；次日到达在到达时间文本前加“次日”前缀一并持久化，所有展示处自动带上标记。
- 班次校验补充：当日到达要求到达晚于发车时间，次日到达允许任意时刻；旧数据无“次日”前缀时默认按当日到达处理。

## 10. 重要控件名称

Designer 中已有的核心控件应保持这些 objectName：

- titleLabel：系统标题
- routeTable：班次表格
- routeNumberEdit：班次号输入框
- departureEdit：出发地输入框
- destinationEdit：目的地输入框
- addRouteButton：添加班次
- editRouteButton：进入修改模式
- saveEditButton：保存修改
- deleteRouteButton：删除选中班次
- searchEdit：搜索关键词
- searchButton：执行搜索
- showAllButton：显示全部
- horizontalLayout：原有班次输入栏

V0.7 在 widget.cpp 中动态创建以下控件：

- departureTimeEdit：发车时间
- priceEdit：票价
- remainingEdit：余票

注意：不要在 Designer 中创建同名控件，否则可能产生重复控件或查找错误。

V0.8 在 widget.cpp 中动态创建以下售票控件：

- ticketPanel：售票中心容器
- selectedRouteLabel：当前选中班次摘要
- ticketQuantitySpin：交易票数（隐藏原生小箭头，可直接输入）
- quantityStepButton：独立的减号/加号按钮，范围为 1～99
- sellTicketButton：售票按钮
- refundTicketButton：退票按钮
- routeCountLabel：班次数统计
- remainingTotalLabel：总余票统计
- soldTotalLabel：累计销量统计
- revenueTotalLabel：营业额统计

V0.9 在 widget.cpp 中动态创建以下驾驶舱控件：

- heroPanel、heroTitle、heroSubtitle、systemBadge：品牌头部与运行状态
- statsPanel：运营指标卡容器
- lowStockLabel：库存预警指标
- contentTabs：班次运营/交易流水页签
- transactionTable：交易流水表格
- transactionSearchEdit：流水即时搜索
- exportButton：导出 CSV
- clearHistoryButton：清空流水


V0.10 在 widget.cpp 中动态创建以下多页面与总览控件：

- overviewPage、routePage、ticketPage、transactionPage：四个独立业务页面
- occupancyProgress：整体售票率进度条
- overviewInsightLabel：自动运营建议
- overviewPanelCard：总览信息卡
- overviewShortcutButton：总览快捷导航按钮
- ticketRouteCombo：售票中心可搜索班次选择器

V0.11 在 widget.cpp 中动态创建以下实名订单控件：

- passengerPanel：实名旅客信息卡片
- passengerNameEdit：旅客姓名
- passengerIdEdit：18 位身份证号
- passengerPhoneEdit：11 位手机号
- orderPage：订单管理页面
- orderTable：实名订单表格
- orderSearchEdit：订单即时搜索
- refundOrderButton：按订单退票按钮
- departureDateEdit：发车日期（日历选择）
- arrivalTimeEdit：到达时间
- arrivalDayCombo：到达日（当日到达 / 次日到达），V0.12 补充
- seatTypeCombo：席位类型选择器
- remainingEdit：席位座位总数输入；余票由累计已售自动推导
- seatPage：独立席位库存页面
- seatServiceCombo：席位页班次选择器
- seatInventoryTable：席位库存明细表
- ticketSeatCombo：售票中心席位选择器
- addSeatButton、saveSeatButton、deleteSeatButton：席位库存维护操作

## 11. 构建与运行注意事项

- 正常顺序：CMake Configure、CMake Build、CMake Run Without Debugging。
- 在命令面板中优先使用带有 CMake 前缀的运行命令，不要误用普通 Debug 配置。
- 若链接时报 Permission denied，通常是旧的 bus_ticket_system.exe 仍在运行；先关闭程序再构建。
- 若提示缺少 Qt6Core.dll，应通过配置好 Qt 环境的 CMake Tools 启动，或部署 Qt 运行库。
- 若 Windows 提示应用程序控制策略阻止文件，这是系统安全策略问题，不是 C++ 编译错误，不要贸然关闭全部安全保护。
- PowerShell 手动运行项目根目录下的程序时，命令为：.\build\bus_ticket_system.exe

## 12. 最近变更

### 2026-08-31

- 完成 V0.7 六字段班次管理功能。
- 为 widget.cpp 的主要功能板块添加中文说明和分区注释。
- 重新编译成功，生成并链接 bus_ticket_system.exe。
- 用户已确认第七版效果符合预期。
- 创建本文件作为项目长期上下文。

- 完成 V0.7 Git 提交，提交号为 784cfd5。
- 开始 V0.8，新增售票中心、售票、退票、余票联动和经营统计。
- V0.8 交易统计已经接入 QSettings 自动保存与读取。
- V0.8 新增代码继续使用中文功能分区注释。
- 售票按钮跟随表格实际选择状态启用或禁用，清除选择后不会残留旧状态。
- 将数量控件原生小箭头改为较大的减号/加号按钮，并在上下限自动禁用。

- 完成 V0.8 Git 提交，提交号为 addfa4f。
- 开始 V0.9 专业化改造：建立运营驾驶舱视觉体系和双页签信息架构。
- 新增库存预警、交易流水持久化、流水搜索、清空和 CSV 导出。
- 修正页签与班次表格的布局所有权顺序，保证班次表格正确显示。
- 所有 V0.9 新功能均按模块添加中文说明注释。
- 将旧的 widget.cpp 内联深色样式迁移到 styles/app.qss，通过 Qt 资源系统全局加载。
- 完成深蓝品牌头部、白色指标卡、浅灰工作区、专业表格、输入焦点、按钮悬停/按下/禁用和语义色升级。
- 为搜索、刷新、添加、编辑、保存、删除、售票、退票、导出和清空操作补充 Qt 标准图标与提示。
- CMake 明确要求 Qt 6.8，并通过 qt_add_resources 将 QSS 编译进可执行文件；全部现有 objectName 保持不变。
- 完成 V0.9 Git 提交，提交号为 903d24f。
- 开始 V0.10 多页面改造：采用左侧导航和四个独立工作页面。
- 新增整体售票率、运营建议、总览快捷入口和班次双击售票跳转。
- 修正浅色主题下库存状态文字颜色，提高对比度和可读性。
- 修复 West 页签默认旋转中文的问题，新增 HorizontalTabBar，使左侧导航图标与文字保持横向排列。
- 修复班次管理页只显示表格的问题，将搜索、操作和六字段录入工具真正移动到管理页面。
- 售票中心增加班次模糊搜索与直接选择，消除必须先去管理页选班次的操作绕行。
- 完成 V0.10 Git 提交，提交号为 93a1111。
- 开始 V0.11 实名订单管理：增加旅客资料校验、唯一订单号、订单持久化、搜索和按订单退票。
- 完善 V0.11 班次模型：新增发车日期、到达时间、席位类型、座位总数与席位独立余票。
- 班次数指标改为按“车次号 + 发车日期”去重，库存预警改为席位级统计。
- 订单扩展为十二列，并兼容升级前十列订单数据。
- 按用户反馈重构 V0.11：班次管理移除席位、票价和容量输入，新增独立席位库存页面。
- 左侧导航升级为运营总览、班次管理、席位库存、售票中心、订单管理、交易流水六页面。
- 班次表对同一车次日期的底层多席位记录合并显示，保留既有数据和订单兼容性。
- 售票中心增加班次、席位两级联动，未配置席位的班次明确提示并禁止售票。

### 2026-09-04

- 开始 V0.12，完成订单组合筛选、按订单改签、站点字典和班次逻辑启停。
- V0.12 在 D 盘独立英文盘符构建目录使用 Qt 6.11.2 / MinGW 13.1 完成配置、编译和链接。
- V0.12 使用 Qt offscreen 平台隐藏启动 4 秒，程序正常保持事件循环。
- 调整站点下拉框最小宽度和表单列伸缩比例，避免较长地名被截断。
- 窗口标题版本号对齐为 V0.12；搜索框提示文案改为“搜索车次号、发车日期、出发地、目的地或时刻”，与实际搜索范围一致。
- 到达时间旁新增「当日到达 / 次日到达」下拉，支持 23:00 发车、次日 06:00 到达的跨天班次；到达时间校验区分当日与次日。
- 完成五项业务一致性修复：移除班次级退票入口，所有退票统一按订单办理。
- 有订单或流水历史的班次禁止修改身份信息和物理删除，只允许逻辑停用；筛选班次或订单时会先清除旧选择，禁止操作隐藏行。
- 售票、改签、退票统一保存班次、订单、流水和站点并检查落盘结果；保存失败会回滚本次库存、订单与流水内存变更。
- 班次底层数据统一按发车日期、发车时间、班次号和席位稳定排序，班次管理、席位库存和售票中心共享一致的时间顺序。
- 站点字典改用 Qt 中文区域校对规则，新增站点和程序启动加载时均按拼音字典序排列。
- 开始 V0.13：售票确认前新增 2+3 布局可视化座位图，支持按购票数量多选、已售禁用、选择进度和状态图例。
- 订单表新增座位号并持久化；退票通过订单状态释放座位，改签目标班次时重新选座；旧订单自动预留累计已售数量，防止重复分配。
- 座位图沿用深蓝、白色、浅灰主题，并补充大点击目标、悬停/选中/已售状态及无障碍名称。
- 订单管理重构为紧凑主列表加行展开详情：主表移除数量、单价、证件号、手机号和座位号列；证件号、手机号、座位号在展开面板展示，数量和单价仅保留为内部计算数据。
- 订单长文本增加完整 tooltip，操作列固定为图标按钮，异常或未分配座位号统一显示短横线。
- 停运班次改为整行浅红底和深红文字警示，重新启用后恢复正常样式。
- 售票确认框扩大，并修复多参数占位符导致数量、应收金额错位或显示不完整的问题。

- 开始 V0.14：新增停运、售罄、低库存和临发预警，支持双击定位班次。
- 售票与改签统一排除已发车及发车前 15 分钟班次；同一证件禁止重复购买同日同车次有效订单。
- 退票增加 48/24 小时手续费分档，确认框和流水展示手续费及实际退款。
- 运营总览新增近七日订单趋势与热门班次 TOP3；订单和流水支持导出当前筛选结果。

- 修复 V0.14 已发车班次只有业务限制、没有视觉反馈的问题：班次管理整行使用浅玫红底和深红文字，并在总览预警中增加“已发车”项目。

- 班次录入新增历史模板复用：输入已有车次号后自动带出最近一次的出发地、目的地、发车时间和到达时间，同时保留当前发车日期。
- 席位库存新增历史模板复用：选择班次和席位类型后，自动带出同车次最近一次相同席位的票价与座位总数，不复制销量和余票。

- 开始 V0.15：新增同车次跨日期整套席位配置继承，添加班次后无需再逐个手动提交席位。

- 开始 V0.16：运营总览增加数据安全工具；备份覆盖班次、席位、订单、流水和站点，恢复前强制生成安全备份。
- 数据体检只读检查库存恒等式、重复班次席位、站点字典、日期时间、订单金额、订单关联和座位冲突。
- 统一业务保存增加最近可靠快照，落盘失败时自动恢复并重新加载界面；顶部系统徽标提供成功或失败反馈。
- 修复席位库存自动填充被残留 currentRow 阻断的问题；切换班次后，已有席位自动选中并回填，未配置席位从最近一期历史模板带出票价和容量。

## 13. 验证记录

- 2026-08-31：V0.7 在添加注释后重新构建成功。
- 2026-09-02：V0.11 在独立 `build-v11-check` 目录完成 CMake 配置、编译和链接；离屏启动 3 秒正常，窗口初始化未崩溃。
- 2026-09-02：原 `build` 目录编译阶段通过，因用户当时仍运行旧程序，最终覆盖 exe 时出现预期的 `Permission denied`；关闭旧窗口后正常重新构建即可。
- 2026-09-02：日期、到达时间、多席位价格/座位数/余票及订单精确退票升级后，在 `build-v11-check` 完成编译、链接和三秒离屏启动测试。
- 2026-09-02：班次与席位职责分离重构完成，在 `build-v11-check` 编译、链接成功，并完成四秒离屏启动测试。
- 2026-08-31：用户已人工验证第七版界面和功能。
- 2026-08-31：V0.8 完成 CMake 编译和链接，生成 bus_ticket_system.exe。
- 2026-08-31：数量减号/加号交互修复后再次编译成功。
- 2026-08-31：V0.9 修正 CSV 转义与流水调用后编译、链接成功。
- 2026-08-31：V0.9 页签布局修正后再次编译成功。
- 2026-08-31：V0.9 使用 Qt offscreen 平台完成三秒启动冒烟测试，程序正常进入事件循环。
- 2026-08-31：V0.9 商业视觉升级后 CMake 自动重新配置、资源编译及 MinGW 链接成功。
- 2026-08-31：确认 :/bus_ticket_system/styles/app.qss 已写入 Qt 资源，离屏启动三秒正常进入事件循环。
- 2026-08-31：V0.10 四页面重构及运营健康度功能完成 MinGW 编译与链接。
- 2026-08-31：补充 Qt 运行库路径后，V0.10 使用 offscreen 平台完成四秒启动冒烟测试。
- 2026-08-31：横排侧边导航修复后重新编译、链接成功，并完成三秒离屏启动测试。
- 2026-08-31：班次管理工具区与售票中心独立选班次功能完成编译、链接和三秒离屏启动测试。
- 2026-09-04：窗口标题与搜索框文案修正、到达时间次日到达功能完成后，在 `build` 目录使用 Qt 6.11.2 / MinGW 13.1 编译、链接成功，并离屏启动 6 秒无崩溃。
- 2026-09-04：五项业务一致性修复在 D 盘独立 `build-fix5` 目录完成编译、链接，并通过四秒离屏启动测试。
- 2026-09-04：班次时间排序与站点拼音排序在 D 盘独立 `build-sort-check` 目录完成编译、链接，并通过四秒离屏启动测试。
- 2026-09-04：V0.13 可视化座位选择在 D 盘独立 `build-seatmap-check` 目录完成编译、链接，并通过四秒离屏启动测试。
- 2026-09-04：订单行展开、停运红色警示及售票确认框修复在 D 盘 `build-order-expand-check` 完成编译、链接，并通过四秒离屏启动测试。
- 2026-09-04：V0.14 在 D 盘纯英文映射构建目录使用 Qt 6.11.2 / MinGW 13.1 编译、链接成功，并通过五秒离屏启动测试。
- 2026-09-04：已发车班次颜色与预警补丁在 D 盘验证目录重新编译、链接成功，并通过四秒离屏启动测试。
- 2026-09-04：班次与席位历史模板自动填充功能重新编译、链接成功，并通过四秒离屏启动测试。
- 2026-09-07：V0.15 整套席位继承功能在 D 盘验证目录使用 Qt 6.11.2 / MinGW 13.1 编译、链接成功，并通过四秒离屏启动测试。
- 2026-09-07：V0.16 数据安全功能在 D 盘 `build-v16-check` 使用 Qt 6.11.2 / MinGW 13.1 编译、链接成功，并通过四秒离屏启动测试。
- 2026-09-07：席位库存自动填充修复后重新编译、链接成功，并通过四秒离屏启动测试。
- 待办：用户人工验证备份文件生成、恢复确认及数据体检报告的交互效果。
